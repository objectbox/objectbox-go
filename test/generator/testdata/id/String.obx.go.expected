// Code generated by ObjectBox; DO NOT EDIT.

package object

import (
	"github.com/google/flatbuffers/go"
	"github.com/objectbox/objectbox-go/objectbox"
	"github.com/objectbox/objectbox-go/objectbox/fbutils"
	"strconv"
)

type string_EntityInfo struct {
	Id  objectbox.TypeId
	Uid uint64
}

var StringBinding = string_EntityInfo{
	Id:  4,
	Uid: 6044372234677422456,
}

// String_ contains type-based Property helpers to facilitate some common operations such as Queries.
var String_ = struct {
	Id *objectbox.PropertyString
}{
	Id: &objectbox.PropertyString{
		Property: &objectbox.Property{
			Id: 1,
		},
	},
}

// GeneratorVersion is called by the ObjectBox to verify the compatibility of the generator used to generate this code
func (string_EntityInfo) GeneratorVersion() int {
	return 1
}

// AddToModel is called by the ObjectBox during model build
func (string_EntityInfo) AddToModel(model *objectbox.Model) {
	model.Entity("String", 4, 6044372234677422456)
	model.Property("Id", objectbox.PropertyType_Long, 1, 8274930044578894929)
	model.PropertyFlags(objectbox.PropertyFlags_ID)
	model.EntityLastPropertyId(1, 8274930044578894929)
}

// GetId is called by the ObjectBox during Put operations to check for existing ID on an object
func (string_EntityInfo) GetId(object interface{}) (uint64, error) {
	return strconv.ParseUint(object.(*String).Id, 10, 64)
}

// SetId is called by the ObjectBox during Put to update an ID on an object that has just been inserted
func (string_EntityInfo) SetId(object interface{}, id uint64) error {
	object.(*String).Id = strconv.FormatUint(id, 10)
	return nil
}

// Flatten is called by the ObjectBox to transform an object to a FlatBuffer
func (string_EntityInfo) Flatten(object interface{}, fbb *flatbuffers.Builder, id uint64) {

	// build the FlatBuffers object
	fbb.StartObject(1)
	fbutils.SetUint64Slot(fbb, 0, id)
}

// ToObject is called by the ObjectBox to load an object from a FlatBuffer
func (string_EntityInfo) ToObject(bytes []byte) interface{} {
	table := &flatbuffers.Table{
		Bytes: bytes,
		Pos:   flatbuffers.GetUOffsetT(bytes),
	}

	return &String{
		Id: strconv.FormatUint(table.GetUint64Slot(4, 0), 10),
	}
}

// MakeSlice is called by the ObjectBox to construct a new slice to hold the read objects
func (string_EntityInfo) MakeSlice(capacity int) interface{} {
	return make([]*String, 0, capacity)
}

// AppendToSlice is called by the ObjectBox to fill the slice of the read objects
func (string_EntityInfo) AppendToSlice(slice interface{}, object interface{}) interface{} {
	return append(slice.([]*String), object.(*String))
}

// Box provides CRUD access to String objects
type StringBox struct {
	*objectbox.Box
}

// BoxForString opens a box of String objects
func BoxForString(ob *objectbox.ObjectBox) *StringBox {
	return &StringBox{
		Box: ob.InternalBox(4),
	}
}

// Put synchronously inserts/updates a single object.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the String.Id property on the passed object will be assigned the new ID as well.
func (box *StringBox) Put(object *String) (string, error) {
	if id, err := box.Box.Put(object); err != nil {
		return "", err
	} else {
		return strconv.FormatUint(id, 10), nil
	}
}

// PutAsync asynchronously inserts/updates a single object.
// When inserting, the String.Id property on the passed object will be assigned the new ID as well.
//
// It's executed on a separate internal thread for better performance.
//
// There are two main use cases:
//
// 1) "Put & Forget:" you gain faster puts as you don't have to wait for the transaction to finish.
//
// 2) Many small transactions: if your write load is typically a lot of individual puts that happen in parallel,
// this will merge small transactions into bigger ones. This results in a significant gain in overall throughput.
//
//
// In situations with (extremely) high async load, this method may be throttled (~1ms) or delayed (<1s).
// In the unlikely event that the object could not be enqueued after delaying, an error will be returned.
//
// Note that this method does not give you hard durability guarantees like the synchronous Put provides.
// There is a small time window (typically 3 ms) in which the data may not have been committed durably yet.
func (box *StringBox) PutAsync(object *String) (string, error) {
	if id, err := box.Box.PutAsync(object); err != nil {
		return "", err
	} else {
		return strconv.FormatUint(id, 10), nil
	}
}

// PutAll inserts multiple objects in single transaction.
// In case Ids are not set on the objects, they would be assigned automatically (auto-increment).
//
// Returns: IDs of the put objects (in the same order).
// When inserting, the String.Id property on the objects in the slice will be assigned the new IDs as well.
//
// Note: In case an error occurs during the transaction, some of the objects may already have the String.Id assigned
// even though the transaction has been rolled back and the objects are not stored under those IDs.
//
// Note: The slice may be empty or even nil; in both cases, an empty IDs slice and no error is returned.
func (box *StringBox) PutAll(objects []*String) ([]string, error) {
	ids, err := box.Box.PutAll(objects)
	if err != nil || len(ids) == 0 {
		return []string{}, err
	}

	var stringIds = make([]string, len(ids))
	for i, id := range ids {
		stringIds[i] = strconv.FormatUint(id, 10)
	}

	return stringIds, nil
}

// Get reads a single object.
//
// Returns nil (and no error) in case the object with the given ID doesn't exist.
func (box *StringBox) Get(id string) (*String, error) {
	idUint64, parseErr := strconv.ParseUint(id, 10, 64)
	if parseErr != nil {
		return nil, parseErr
	}

	object, err := box.Box.Get(idUint64)

	if err != nil {
		return nil, err
	} else if object == nil {
		return nil, nil
	}
	return object.(*String), nil
}

// Get reads all stored objects
func (box *StringBox) GetAll() ([]*String, error) {
	objects, err := box.Box.GetAll()
	if err != nil {
		return nil, err
	}
	return objects.([]*String), nil
}

// Remove deletes a single object
func (box *StringBox) Remove(object *String) (err error) {
	idUint64, parseErr := strconv.ParseUint(object.Id, 10, 64)
	if parseErr != nil {
		return parseErr
	}

	return box.Box.Remove(idUint64)
}

// Creates a query with the given conditions. Use the fields of the String_ struct to create conditions.
// Keep the *StringQuery if you intend to execute the query multiple times.
// Note: this function panics if you try to create illegal queries; e.g. use properties of an alien type.
// This is typically a programming error. Use QueryOrError instead if you want the explicit error check.
func (box *StringBox) Query(conditions ...objectbox.Condition) *StringQuery {
	return &StringQuery{
		box.Box.Query(conditions...),
	}
}

// Query provides a way to search stored objects
//
// For example, you can find all String which Id is lower than 100:
// 		box.Query(String_.Id.LessThan(100)).Find()
type StringQuery struct {
	*objectbox.Query
}

// Find returns all objects matching the query
func (query *StringQuery) Find() ([]*String, error) {
	objects, err := query.Query.Find()
	if err != nil {
		return nil, err
	}
	return objects.([]*String), nil
}
